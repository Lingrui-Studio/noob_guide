[project]
name = "noob_guide"
version = "0.1.0"
description = "Noob guid for those who want to join Lingrui Studio"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.47",
]
